# Локальная отладка, в том числе Windows.
# У JAX нет GPU-сборок под Windows, а wheels jaxlib для win_amd64 публикуются
# только для CPU и только до 0.9.2 — отсюда верхняя граница.
jax[cpu]>=0.4.38,<=0.9.2
flax>=0.10.2
distrax>=0.1.5
ml_collections>=1.1.0
ogbench==1.1.4
gymnasium>=0.29.1
scipy>=1.11
pandas>=2.0
matplotlib>=3.8
tqdm>=4.66
